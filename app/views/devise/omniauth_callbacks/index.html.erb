<script>
  if(window.opener) {
    var postReferenceId = window.opener.postReferenceId

    if (postReferenceId){
      window.opener.location = "/pub/" + postReferenceId;
    } else{
      window.opener.location.reload(true);
    }

    window.close();
  }
</script>
